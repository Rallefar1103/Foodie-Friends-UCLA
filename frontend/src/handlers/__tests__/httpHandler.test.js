import { HTTPHandler } from "../httpHandler";
import { describe, expect, test } from "@jest/globals";

describe("httpHandler module", () => {
  test("should return true", () => {
    expect(true);
  });
});
