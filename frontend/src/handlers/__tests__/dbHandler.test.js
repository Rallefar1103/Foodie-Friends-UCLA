//import { DBHandler } from "../dbHandler";
import { describe, expect, test } from "@jest/globals";

describe("dbHandler module", () => {
  test("should return true", () => {
    expect(true);
  });
});
